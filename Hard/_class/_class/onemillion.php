<!DOCTYPE html>
<body>
<p>O algorítimo seria escrito da seguinte forma:</p>
    </p>
        $arr = array();
        $index = 0;
        for ($i = pow((1*10),9) $i > pow((1*10),6); $i--) {
            if ($index <= pow((1*10),6)) {
                $arr[$index] = $i;
                $index++;
            } 
        }</p>    
</body>
</html>